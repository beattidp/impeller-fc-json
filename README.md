# impeller-fc-json

_Intent is to use generated curve equations to model the blades and shroud of an impeller with a flat hub in FreeCAD, via JSON import into the Parametric_Curve_FP macro._

![Impeller_FreeCAD.png](./Impeller_FreeCAD.png)

A Python script calculates optimized impeller blade curve equations based on maximum inlet height, inlet and outlet radius, number of blades, and blade thickness. The curve equations are written to a JSON file in a format compatible with the Parametric_Curve_FP macro.

Parameters are defined at the beginning of the Python script and these plus some calculated values are written to ```impeller_spec.txt```

---

The generated JSON file contains 4 curves; two sets for leading and trailing edges so the defined thickness of the blades can be factored into the optimized logarithmic spiral profiles. 

These curves must be imported into FreeCAD by running the macro Parametric_Curve_FP four times to create four objects. The objects can be renamed, i.e.
 - blade_spiral_leading
 - hill_spiral_leading
 - blade_spiral_trailing
 - hill_spiral_trailing

Double-click on each of the resulting objects to read in the new data set from JSON using 'Load', select the corresponding definition, and 'Apply'. Verify the result in the model view and repeat for all.

---

Example curves defined in ```formula1.json``` are for a 3D print using 0.4mm nozzle aperture. Blade thickness is a multiple of the aperture; in this example is 1.6mm. The diameter of the hub (base) in this example is 160mm, which is the same as a CD-ROM. Total height at the top of the inlet is 35mm.

### Credit where Credit is Due:
The original code for this application was generated by [Google Antigravity](http://antigravity.google/).  _Thank you!_
